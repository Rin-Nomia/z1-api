name: 同步 z1_mvp 並部署 API

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  SOURCE_REPO: Rin-Nomia/z1_mvp
  HF_USERNAME: RinNomia
  HF_SPACE: z1-tone-api

jobs:
  sync_and_deploy:
    runs-on: ubuntu-latest
    
    steps:
      # 關卡 1：抓取當前 repo (z1-api)
      - name: 關卡 1 - Checkout z1-api
        uses: actions/checkout@v4
        with:
          path: z1-api
      
      # 關卡 2：抓取 z1_mvp
      - name: 關卡 2 - Checkout z1_mvp
        uses: actions/checkout@v4
        with:
          repository: ${{ env.SOURCE_REPO }}
          token: ${{ secrets.GH_PAT }}
          path: z1_mvp
      
      # 關卡 3：複製資料夾（自動化！）
      - name: 關卡 3 - 複製 pipeline, core, configs
        run: |
          echo "📦 開始複製資料夾..."
          
          cp -r z1_mvp/pipeline z1-api/
          echo "✅ pipeline/ 已複製"
          
          cp -r z1_mvp/core z1-api/
          echo "✅ core/ 已複製"
          
          cp -r z1_mvp/configs z1-api/
          echo "✅ configs/ 已複製"
          
          echo ""
          echo "📂 檔案結構："
          ls -lh z1-api/
      
      # 關卡 4：設定 Python
      - name: 關卡 4 - Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      
      # 關卡 5：測試 Pipeline
      - name: 關卡 5 - 測試 Pipeline 是否正常
        working-directory: z1-api
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "📦 安裝依賴..."
          pip install -r requirements.txt
          
          echo ""
          echo "🧪 開始測試 Pipeline"
          echo "================================"
          
          python << 'PYTEST'
          from pipeline.z1_pipeline import Z1Pipeline
          import json
          
          print("初始化 Pipeline...")
          pipeline = Z1Pipeline(debug=True)
          print("✅ Pipeline 初始化成功")
          print()
          
          # 測試：正常長度文字
          print("🧪 測試：正常長度文字")
          print("-" * 50)
          test_text = "I don't know what to do anymore. Everything I try fails. Can someone please help me?"
          print(f"測試文字：{test_text}")
          print()
          
          result = pipeline.process(test_text)
          
          if result.get('error'):
              print(f"❌ Pipeline 測試失敗")
              print(f"   錯誤原因：{result.get('reason')}")
              exit(1)
          else:
              print(f"✅ Pipeline 測試成功！")
              print()
              print(f"📊 分析結果：")
              print(f"   類型：{result['freq_type']}")
              print(f"   信心值：{result['confidence']['final']:.2f}")
              print(f"   場景：{result['output']['scenario']}")
              print()
              print(f"🎉 測試通過！API 可以正常運作")
          PYTEST
      
      # 關卡 6：部署到 HuggingFace（修正版）
      - name: 關卡 6 - 部署到 HuggingFace
        working-directory: z1-api
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          echo "📦 安裝 HuggingFace Hub..."
          pip install huggingface_hub
          
          echo ""
          echo "🔐 登入 HuggingFace..."
          python -m huggingface_hub.commands.huggingface_cli login --token $HF_TOKEN
          
          echo ""
          echo "📤 上傳到 HuggingFace Space..."
          python -m huggingface_hub.commands.huggingface_cli upload \
            ${{ env.HF_USERNAME }}/${{ env.HF_SPACE }} \
            . \
            --repo-type=space \
            --commit-message="Auto deploy Z1 API [$(date +'%Y-%m-%d %H:%M:%S')]"
          
          echo ""
          echo "================================"
          echo "✅ 部署完成！"
          echo "================================"
          echo ""
          echo "📍 API 端點："
          echo "   健康檢查：https://${{ env.HF_USERNAME }}-${{ env.HF_SPACE }}.hf.space/health"
          echo "   API 文件：https://${{ env.HF_USERNAME }}-${{ env.HF_SPACE }}.hf.space/docs"
          echo "   分析端點：https://${{ env.HF_USERNAME }}-${{ env.HF_SPACE }}.hf.space/api/v1/analyze"
          echo ""
          echo "🧪 測試指令："
          echo "   curl https://${{ env.HF_USERNAME }}-${{ env.HF_SPACE }}.hf.space/health"
          echo ""
          echo "⏳ 注意：HuggingFace Space 需要 2-5 分鐘啟動"
          echo "================================"
